import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false, //Turning strict mode to "true" causes rendering problems in OpenLayers
};

export default nextConfig;
